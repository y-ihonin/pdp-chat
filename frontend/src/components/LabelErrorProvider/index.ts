import LabelErrorProvider from "./LabelErrorProvider";
import { LabelErrorProviderProps } from "./LabelErrorProvider.interface";

export type { LabelErrorProviderProps };
export default LabelErrorProvider;
